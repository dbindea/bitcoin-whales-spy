export default class SmsService {
  async notifySmsSubscribers(phoneArray, unnotifiedTransactions) {
    const notifiedTransactions = [];

    return notifiedTransactions;
  }

  async sendSms(phone, text) {
    let status = false;

    return status;
  }
}
